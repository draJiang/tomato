export type showMaskType = {
    'status': string,
    'tomato': tomatosType,
    'websiteType': string,
    'showMask': boolean
}

export type tomatosType = {
    'total': number,
    'balance': number
}